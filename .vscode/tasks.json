{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "/usr/bin/clang",
            "args": [
                "-fopenmp",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${file}",
                "-L/usr/local/lib",
                "-lomp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Generated task for Clang with OpenMP"
        }
    ]
}
